CREATE TABLE def_servpath_urn_smartsantander_testbed_357_santander_soundacc
(
  recvTimeTs LONGTEXT,
  recvTime   TIMESTAMP,
  entityId   LONGTEXT,
  entityType LONGTEXT,
  attrName   VARCHAR(255),
  attrType   VARCHAR(255),
  attrValue  DOUBLE,
  attrMd     LONGTEXT
);

CREATE TABLE def_servpath_meteolo_meteolo
(
  recvTimeTs LONGTEXT,
  recvTime   TIMESTAMP,
  entityId   LONGTEXT,
  entityType LONGTEXT,
  attrName   VARCHAR(255),
  attrType   VARCHAR(255),
  attrValue  DOUBLE,
  attrMd     LONGTEXT
);